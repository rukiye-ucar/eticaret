
// Web'deki :root CSS değişkenlerinin birebir karşılığı
export const colors = {
    bgBase: '#2d2250',  // --color-bg-base      (ana arka plan)
    primary: '#f9b17a',  // --color-primary       (vurgu / aksiyon)
    bgContainer: '#576f9d',  // --color-bg-container  (kart / panel)
    textBase: '#ffffff',  // --color-text-base     (metin / ikon)
    secondary: '#244f69',  // --color-secondary     (yardımcı ton)
};

export const radius = {
    card: 20,
    button: 12,
    input: 20,
};

export const spacing = {
    xs: 8,
    sm: 12,
    md: 16,
    lg: 24,
    xl: 30,
};
